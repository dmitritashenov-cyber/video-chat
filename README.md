# Video Chat Application

Веб-приложение для видеозвонков с поддержкой WebRTC и текстового чата.

## Возможности

- 🔐 Регистрация и аутентификация пользователей
- 🎥 Видеозвонки через WebRTC (peer-to-peer)
- 💬 Текстовый чат в реальном времени
- 🔗 Отправка ссылок на комнаты другим пользователям
- 📱 Адаптивный интерфейс

## Технологии

- **Backend**: FastAPI, WebSockets
- **Frontend**: HTML, JavaScript, WebRTC API
- **Deployment**: Render (готов к развертыванию)

## Установка и запуск локально

### Требования

- Python 3.10 или выше
- pip

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd video-chat
```

2. Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Запустите сервер:
```bash
python server.py
# или
uvicorn server:app --host 0.0.0.0 --port 8000
# или используйте скрипт
./start.sh
```

5. Откройте браузер и перейдите по адресу:
```
http://localhost:8000
```

## Развертывание на Render

Приложение готово к развертыванию на Render. Вы можете использовать один из следующих способов:

### Способ 1: Использование render.yaml (рекомендуется)

1. Загрузите ваш код в GitHub/GitLab/Bitbucket
2. В Render Dashboard:
   - Нажмите "New +" → "Blueprint"
   - Подключите ваш репозиторий
   - Render автоматически обнаружит `render.yaml` и создаст сервис

### Способ 2: Ручная настройка

1. В Render Dashboard создайте новый Web Service
2. Подключите ваш репозиторий
3. Настройки:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn server:app --host 0.0.0.0 --port $PORT`
   - **Environment**: Python 3
   - **Port**: Render автоматически установит переменную `PORT`

### Важные замечания для Render

- ✅ Переменная окружения `PORT` устанавливается автоматически Render
- ✅ Приложение настроено на использование `0.0.0.0` как хоста
- ✅ Health check endpoint доступен по `/health`
- ⚠️ Данные пользователей и комнат хранятся в памяти и теряются при перезапуске

## Структура проекта

```
video-chat/
├── server.py              # Основной серверный файл (FastAPI)
├── requirements.txt       # Зависимости Python
├── render.yaml           # Конфигурация для Render
├── start.sh              # Скрипт запуска
├── static/               # Статические файлы
│   ├── login.html        # Страница входа
│   ├── dashboard.html    # Дашборд пользователя
│   └── room.html         # Страница видеокомнаты
└── README.md            # Документация
```

## API Endpoints

- `GET /` - Главная страница (логин)
- `POST /login` - Аутентификация/регистрация
- `GET /dashboard?user=<username>` - Дашборд пользователя
- `POST /send_link` - Отправка ссылки на комнату
- `WebSocket /ws/{room_id}` - WebSocket соединение для видеокомнаты
- `GET /health` - Health check endpoint

## Улучшения в коде

Следующие улучшения были внесены в код:

- ✅ Добавлено логирование для отслеживания работы приложения
- ✅ Улучшена обработка ошибок во всех endpoints
- ✅ Добавлена валидация входных данных
- ✅ Улучшена обработка WebSocket соединений
- ✅ Добавлен CORS middleware для кросс-доменных запросов
- ✅ Добавлен health check endpoint для мониторинга
- ✅ Улучшена структура кода с комментариями
- ✅ Поддержка переменных окружения для гибкой конфигурации
- ✅ Безопасная обработка отключений клиентов

## Безопасность

⚠️ **Важно**: Текущая версия использует простую аутентификацию без шифрования паролей. Для продакшена рекомендуется:

- Хеширование паролей (bcrypt, argon2)
- Использование сессий/JWT токенов
- HTTPS/WSS для всех соединений
- База данных для хранения пользователей
- Rate limiting для API endpoints

## Лицензия

Этот проект доступен для использования и модификации.

## Поддержка

При возникновении проблем проверьте:
- Логи приложения
- Настройки портов и хостов
- Доступность необходимых портов в сети/Render
- Браузерные консоли для ошибок WebRTC
